# Test data

## specification_a

The files here aren't true planning spec schema files but are pretty similar. They have been modified to make them useful for unittests. They aer mainly the original files with some fields removed.

## specification_b

For very simple module -> field -> component tests

