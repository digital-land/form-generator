"""
Classes to define overrides to `SchemaNode` nodes for the User Interface.

These are just for the unittests.
"""


class PhoneNumber:
    _display = "A Phone Number"  # override copy shown to user
